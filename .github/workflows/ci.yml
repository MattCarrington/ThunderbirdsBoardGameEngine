name: CI

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  build:
    name: Build .NET Solution
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.x'

      - name: Restore & Build
        run: |
          dotnet restore ThunderbirdsBoardGameEngine.sln
          dotnet build ThunderbirdsBoardGameEngine.sln --configuration Release --no-restore

  docker-build:
    name: Build Docker Image
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Build Docker Image
        run: docker build -t thunderbirds-api -f ThunderbirdsBoardGameEngine.GameData.Api/Dockerfile .

      - name: Save Docker Image
        run: docker save thunderbirds-api -o thunderbirds-api.tar
      
      - name: Upload Image Artifact
        uses: actions/upload-artifact@v3
        with:
          name: thunderbirds-api-image
          path: thunderbirds-api.tar

  unit-tests:
    name: Run Unit Tests
    runs-on: ubuntu-latest
    needs: build

    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.x'

      - name: Run Unit Tests
        run: |
          find . -name "*UnitTests.csproj" -exec dotnet test {} --configuration Release --no-restore \;

  integration-tests:
    name: Run Integration & DataAccess Tests
    runs-on: ubuntu-latest
    needs: [build, docker-build]

    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.x'

      - name: Download Image Artifact
        uses: actions/download-artifact@v3
        with:
          name: thunderbirds-api-image
          path: .
      
      - name: Load Docker Image
        run: docker load -i thunderbirds-api.tar

      - name: Run API Docker Container
        run: |
          docker run -d -p 8080:8080 --name thunderbirds-api-test thunderbirds-api
          sleep 5

      - name: Run Integration & DataAccess Tests
        run: |
          find . -name "*IntegrationTests.csproj" -exec dotnet test {} --configuration Release --no-restore \;
          find . -name "*DataAccessTests.csproj" -exec dotnet test {} --configuration Release --no-restore \;

      - if: always()
        name: Tear Down Docker Container
        run: docker rm -f thunderbirds-api-test
